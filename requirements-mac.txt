# Intel MacBook Pro query runtime. CPU only.
# No faster-whisper, no crawler deps: the Mac never transcribes or crawls.
#
# These pins are the last stack that runs on Intel macOS. PyTorch stopped
# shipping Intel wheels after 2.2.2, and that build needs NumPy 1.x. SciPy
# 1.18 and current transformers both require a newer PyTorch or NumPy.
# LanceDB 0.26 and later publish no Intel macOS wheel, so the index has to
# be written by this same 0.25.3. Changing any of these requires a Windows
# reindex and a full export.

numpy>=1.26,<2
scipy==1.17.1
torch==2.2.2
requests>=2.31
sentence-transformers==5.7.0
transformers==4.57.6
lancedb==0.25.3
pyarrow>=16.0
pandas>=2.2
fastapi>=0.115
uvicorn>=0.30
